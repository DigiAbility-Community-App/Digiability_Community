{
  "name": "@digiability/moderation",
  "version": "1.0.0",
  "private": true,
  "description": "Shared content moderation package — text screening for chat-svc, forum-svc, and any content-producing route",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "lint": "echo 'no linter configured for moderation package'"
  },
  "dependencies": {
    "obscenity": "^0.4.6"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^26.5.0"
  }
}
